En un archivo de texto separado que debes crear, escribe explicaciones de los siguientes conceptos como si se lo estuvieras explicando a un niño de 12 años. Hacer esto te ayudará a descubrir rápidamente cualquier agujero en tu comprensión.

prototype: Es una propiedad que tienen todos los objetos de JavaScript, el cual representa el prototipo de cada objeto. 

Constructors (de Clases): Las clases sirven para crear muchos objetos que comprarten las mismas propiedades. El constructor crea e inicializa un objeto creado con una clase. 